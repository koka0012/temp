{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/react-native",
      "**/react-native/**"
    ]
  },
  "scripts": {
    "setup": "./scripts/setup.sh",
    "mobile:pod": "yarn workspace @/mobile pod:install"
  },
  "devDependencies": {
    "lerna": "^3.18.1"
  }
}
