{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/react-native",
      "**/react-native/**"
    ]
  },
  "scripts": {
    "setup": "./scripts/setup.sh",
    "mobile:android": "yarn workspace @/mobile android",
    "mobile:ios": "yarn workspace @/mobile ios",
    "mobile:clear": "yarn workspace @/mobile clear",
    "mobile:pod": "yarn workspace @/mobile pod:install"
  },
  "devDependencies": {
    "lerna": "^3.18.1"
  }
}
